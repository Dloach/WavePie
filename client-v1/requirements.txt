# WavePie client-v1 — PC 端客户端
#
# Usage:
#   cd client-v1 && pip install -r requirements.txt
#   python -m src.app

pyyaml>=6.0
pynput>=1.8
pygame>=2.6
pystray>=0.19
pillow>=9.0
pyinstaller>=6.0
bleak>=0.22
